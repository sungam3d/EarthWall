# EarthWall v1.0.07 — Work Plan & Checklist
(Keep this file in the repo. Each session: read this first, tick items, bump notes.)

## Phase 1 — Bug fixes & small features (this session)
- [x] **1.1 Preview stretch bug** — preview renders hardcoded 1280x640 (2:1) but
      "Update now" renders at real screen aspect. Fix: in gui_main_window.py,
      compute preview size from `_current_resolution()` aspect (width 1280,
      height = round(1280 * h/w)); also make `_update_preview_size()` use that
      same aspect instead of `// 2`. Re-render preview when resolution changes.
- [x] **1.2 Window sizing pass** — audit all windows/dialogs so no settings are
      cut off: main window (each tab), gui_city_dialog.py, gui_map_dialog.py.
      Use minimumSizeHint/adjustSize, sensible setMinimumSize, and scroll areas
      where tabs are tall.
- [x] **1.3 Cloud density control** — new slider "Cloud density" (0–100%).
      Implementation: threshold/attenuate the cloud alpha channel, e.g.
      alpha' = clip((alpha - t) / (1 - t)) with t derived from density, so thin
      cloud disappears first and dense cores remain. Keep opacity as overall
      transparency; density controls coverage. Settings key: `cloud_density`.
- [x] **1.4 Night view toggle** — checkbox "Show night side" (settings key
      `night_view`, default on). When off, render full-day map (skip day/night
      mask blend, or mask weight = 1.0 day).

## Phase 2 — Multi-monitor support ✅ COMPLETE
- [x] **2.1 Monitor detection layer** — enumerate QScreens (geometry, position,
      resolution, aspect). Model: virtual desktop bounding box + per-monitor rects
      (handles diagonal/offset layouts like EarthView-04). **Done in monitors.py.**
- [x] **2.2 Settings schema** — `monitors_mode`: "mirror" | "span" | "independent";
      per-monitor map config for independent mode. **Done: defaults in settings.py,
      helpers in monitors.py (monitor_config_for / set_monitor_config).**
- [x] **2.3 Preview UI rework** — screen-area preview showing each monitor as an
      outlined rect inside the virtual desktop grid; two previews (or combined
      grid view) when 2 monitors detected, each at its own aspect.
      **Done in gui_display_widgets.py (ScreenAreaPreview). Handles diagonal
      layouts correctly (verified against EarthView-04 numbers).**
- [x] **2.4 Map placement controls** — zoom slider, pan up/down as well as
      left/right (map offset x/y), draggable red focal-point dot on the preview
      (EarthView style). **DONE: draggable red-dot focal picker updates
      center_lon/center_lat live; zoom slider 50-400% stored in
      monitor_configs[N]["zoom"]; position spinboxes pin exact placement;
      Auto-center button. Per-monitor independent editing done (2.7).**
- [x] **2.5 Void fill** — DONE: colour swatch + image chooser + clear button +
      status label; renderer honours image with aspect-preserving scale+crop
      and per-monitor void fill applied on padded monitors in independent mode.
- [x] **2.6 Renderer changes** — DONE: mirror unchanged, span composes onto
      virtual-desktop canvas with void fill, wallpaper.py spanned=True flag
      sets DE-appropriate options (GNOME/Cinnamon/MATE picture-options=spanned,
      feh --no-xinerama, swaybg fit).
- [x] **2.7 True per-monitor independent editing** — each monitor renders its
      own view at its own dimensions with its own zoom/focal/void. New helpers
      _render_map_image and _render_monitor_view extracted; independent mode
      composites each monitor's render onto the virtual-desktop canvas at its
      virtual position. UI: "Editing monitor" dropdown appears only in
      independent mode; controls route through active monitor's config;
      switching targets reloads control values; per-monitor void fill applies
      to padding when a monitor's zoom < 100%; global void fill still applies
      to gaps between monitors in diagonal layouts. Verified end-to-end.

## Phase 3 — GUI redesign (2026-07-11)
- [x] **3.1 General tab: start-in-tray** — added "Start hidden in the system
      tray" checkbox (sub-option under autostart). Setting start_in_tray;
      gui.py skips window.show() when set and shows a tray balloon instead.
- [x] **3.2 Clouds & Weather tab** — split live clouds, cloud opacity/density,
      night side, and city-weather (temp units) out of General into their own
      tab. Tab order: General, Map & View, Clouds & Weather, Displays, Cities.
- [x] **3.3 Map & View: draggable dot replaces center slider** — moved the
      MapFocalPointPreview (red-dot picker) from Displays to Map & View,
      replacing the old longitude slider (dot does lon AND lat, strictly
      better). Old slider/spinbox kept as hidden widgets for load/save compat;
      preset buttons ("Americas/Atlantic/Asia/Pacific") now move the dot too.
- [x] **3.4 Displays: taller screen area** — screen-area preview min height
      220→340 with stretch, so a whole monitor stays visible when zoom < 100%
      shrinks the map rect. Fixed inverted-zoom bug: map-area rect was
      vw/zoom (grew when zooming out -> wide+short box); now vw*zoom (shrinks
      when zooming out), matching the renderer.
- [x] **3.5 CRITICAL: zoom/position now reach the desktop** — root cause:
      _render_layout() returned None in mirror mode, and render() only
      honoured zoom/pos in span/independent (both need a layout), so the
      Displays zoom + X/Y controls updated the preview but were silently
      dropped from the actual wallpaper render. Fix: _render_layout() now
      returns a layout whenever zoom != 1.0 or map offset != 0 (even in
      mirror mode), and render() treats "mirror + layout" as span (composes
      the single map onto the virtual-desktop canvas honouring zoom/pos/void).
      Default case (mirror, 100%, no offset) still returns None -> fast path.
      Verified: mirror+zoom50 shows void border in the real render output.

## Phase 4 — GUI polish + number inputs (2026-07-11)
- [x] **4.1 Screen-area zoom behaviour** — monitor rectangle now stays a
      FIXED size at every zoom level; only the map rect scales, clipped to
      the widget so overflow (zoom>100%) bleeds off-edge instead of forcing
      a rescale that shrank the monitor. _fit_transform fits only the virtual
      desktop (never the map area); map drawn under a setClipRect; badge
      pinned to the visible region. Verified monitor rect identical at
      50/100/300% zoom.
- [x] **4.2 LabeledSlider widget** — new gui_widgets.LabeledSlider pairs a
      ClickJumpSlider with a synced QSpinBox (two-way, feedback-loop-safe),
      exposing a plain-slider-compatible value()/setValue()/valueChanged API.
      setValue() is silent (load path safe); user edits emit once.
- [x] **4.3 Number inputs everywhere** — converted to LabeledSlider: cloud
      opacity, cloud density, twilight (sharp), night darkness (shadows),
      map zoom. Added dedicated X/Y (lon/lat) number boxes for the map-center
      red dot, synced both ways with dragging. Removed all the old read-only
      "value_label" QLabels these replace.
- [x] **4.4 README** — refreshed: five-tab overview, multi-monitor modes,
      cloud opacity vs density, night toggle, start-in-tray, draggable dot +
      number boxes, crash log; added monitors.py / gui_display_widgets.py to
      the module list; fixed stale "2:1 preview" note.
- [x] **4.5 Code review** — py_compile + pyflakes clean across all modules;
      removed dead imports (QSlider/QDoubleSpinBox in main window, QSlider in
      city dialog, QEvent in widgets, Optional in display widgets) and two
      dead locals (gui_worker temp_units, autostart module_dir). Verified
      _render_layout engagement logic (default=None fast path; zoom/offset/
      independent engage), focal extremes, and silent-load behaviour.

## Reference notes
- Bug repro: Update now → move cloud opacity → preview stretches; Update now fixes.
  Root cause confirmed in code (hardcoded 2:1 preview vs screen-aspect wallpaper).
- User's reference screenshots (EarthView-01..04.jpg) were mentioned but NOT
  received in session 1 — ask user to re-attach before Phase 2.3/2.4.
- Cloud source: clouds.matteason.co.uk alpha PNG, 4096x2048, cached 3h.
- Version bump: earthwall/__init__.py → 1.0.07 when Phase 1 lands.

## Session log
- Session 1 (2026-07-10): Phase 1 COMPLETE (1.1-1.4 implemented + smoke-tested headless). Version bumped to 1.0.07. Next session: start Phase 2.1; ask user to re-attach EarthView-01..04.jpg screenshots.
- Session 2 (2026-07-10): Added first-load spinner with explanatory text (animated dots + "why this is slow" note; stops on first pixmap). Phase 2.1 + 2.2 complete: monitors.py module with Monitor / MonitorLayout dataclasses, detect_layout(), virtual-desktop bounding box (preserves negative offsets for diagonal setups), per-monitor config helpers. Settings schema extended with monitors_mode + monitor_configs. Version 1.0.08. Phase 2.3 blocked on user re-attaching EarthView-01..04.jpg screenshots.
- Session 3 (2026-07-11): Got EarthView screenshots. New "Displays" tab in main window. New gui_display_widgets.py with ScreenAreaPreview (renders virtual desktop + monitors + red-outlined map area, verified against diagonal EarthView-04 numbers) and MapFocalPointPreview (draggable red dot updating center_lon/center_lat live). Mode selector (mirror/span/independent), zoom slider, void-fill colour picker all wired to settings. Monitor detection deferred to showEvent (needs live QApplication). Version 1.0.09. NEXT: 2.6 renderer — teach render.py to compose per-monitor / spanned output, honour zoom + focal, paint void fill; teach wallpaper.py to apply spanned or per-monitor. Also 2.4/2.5 tail: position spinboxes, view list, per-monitor independent editing, void-fill image chooser.
- Session 4 (2026-07-11): Widened Edit City dialog (500→660 min, 720x720 default) to fit its 5-tab strip. Phase 2.6 renderer done: render() accepts monitor_layout + monitors_mode + map_zoom + void_fill_color/image; _compose_multi_monitor() places rendered map onto virtual-desktop canvas with void fill (fast path when map fully covers). Verified: mirror back-compat, span mode with diagonal layout, zoom<100% shows void, zoom>100% covers. GUI: _current_resolution() returns virtual desktop size in span/independent modes so preview matches. RenderWorker takes monitor_layout snapshot (main-thread-captured, no QScreen from worker). Version 1.0.10. NEXT: 2.4/2.5 tail (position spinboxes + view list + void-fill image chooser + per-monitor independent editing), then wallpaper.py DE-specific "spanned" apply option.
- Session 5 (2026-07-11): Phase 2 CORE COMPLETE. 2.4 tail: position spinboxes (Map position X/Y in px, auto-center button) added to Displays tab, persisted per-monitor. 2.5 tail: image chooser (QFileDialog), clear-image button, "using colour" / "using image: X" status label. wallpaper.py set_wallpaper() gained spanned=True flag: sets picture-options=spanned on GNOME/Cinnamon/MATE, --no-xinerama on feh, fit mode on swaybg; worker auto-enables spanned when mode is span/independent. Version 1.0.11. Phase 2 remaining: true per-monitor independent editing (independent currently falls back to span composition), multiple "views" like EarthView's View 1/2 randomization. Both are nice-to-have; nothing blocks daily use.
- Session 6 (2026-07-11): Phase 2 FULLY COMPLETE (2.7). True per-monitor independent rendering: refactored render.py into _render_map_image (single map at any size) + _render_monitor_view (single monitor with its own zoom/focal/void, cropping when zoom>1, per-monitor padding when zoom<1) + independent-mode branch in render() that composes each monitor's render onto virtual-desktop canvas at its true position; gaps between monitors use global void fill. UI: monitor selector combo appears only in independent mode; _active_monitor_index() helper routes all Displays-tab controls (zoom, position, focal, void fill) through the selected monitor's config; switching the selector reloads control values from that monitor. Verified: per-monitor isolation (mon 0 = 150% zoom + focal (0,0), mon 1 = 75% zoom + focal (120,45)), diagonal gap magenta fill, control-value reload on selector change. Version 1.0.12.
- Session 7 (2026-07-11): User reported the mode dropdown only showed "Mirror" on their Windows setup. Code had all 3 items - display bug. Applied belt-and-braces fix: setMaxVisibleItems(10), setMinimumContentsLength(38), and explicit QListView() as the popup view (avoids QScrollArea viewport clip inheritance on some Windows styles). Also renamed labels to clearer names the user actually expected: "Mirror", "Stretch", "Custom per-monitor" (data values unchanged - still "mirror"/"span"/"independent" on the wire). Version 1.0.13.
- Session 8 (2026-07-11): CRASH FIX. App is Linux-only (Windows references in session 7 were a mistaken tangent - disregard). v1.0.13's crash-on-load was the bare QListView() set as the mode combo's popup view: PySide6 lets the Python wrapper be garbage-collected while Qt still holds the C++ pointer -> segfault under real xcb (invisible under offscreen platform, which is why earlier tests missed it). REMOVED the setView(QListView()) call; the setMaxVisibleItems(10) + setMinimumContentsLength(38) setters fix the one-row-popup display bug on their own. Added a global crash logger (gui.py _install_crash_logger): sys.excepthook writes full tracebacks to ~/.config/earthwall/earthwall_crash.log + shows a QMessageBox, so future crashes are diagnosable instead of silent. Hardened detect_layout() against zero-size/phantom screens (common mid-"Extend displays" toggle) and null primaryScreen. Wrapped both display-widget paintEvents in try/except (a raised exception in paintEvent can hard-crash Qt). Verified the full load + popup + extended-desktop + per-monitor-edit + focal-drag + repaint path under REAL xcb (xvfb + libxcb-cursor0) with zero crashes and empty crash log. Version 1.0.14.
  NOTE for future sessions: wallpaper.py is Linux-DE only (gnome/kde/cinnamon/xfce/mate/sway/feh) - no Windows/macOS paths, and that's correct/intended.
- Session 9 (2026-07-11): Phase 3 GUI redesign complete (see Phase 3 section). Biggest win: fixed the long-standing bug where Displays-tab zoom/X/Y never affected the actual desktop (only the preview) - _render_layout() now engages the placement render path for any non-default zoom/offset, and render() treats mirror+layout as span. Also split Clouds & Weather into its own tab, moved the draggable focal dot to Map & View (replacing the longitude slider), added start-in-tray option, made the Displays screen-area preview taller, and fixed the inverted-zoom bug that made it wide+short below 100%. Version 1.0.15. Verified end-to-end under real xcb.
- Session 10 (2026-07-11): Phase 4 (see section). Fixed screen-area zoom so the monitor rect never resizes (only the map scales, clipped to widget) - the wide+short breakage below 100% is gone. Added LabeledSlider (slider + synced number box) and rolled it out to cloud opacity/density, twilight, night darkness, and zoom; added X/Y number boxes for the map-center dot. Refreshed README with all Phase 2-4 features. Code review: pyflakes clean, removed dead imports/locals. Version 1.0.16. Verified end-to-end under real xcb.
- Session 11 (2026-07-11): Fixed screen-area zoom mask model. User's correct mental model: grey box = monitor (a mask/viewport), red box = the WHOLE map image (always fully outlined, thumbnail always fills it entirely). Map drawn BEHIND the screen; inside the monitor you see the map at full brightness, outside you see it dimmed (0.35) plus the red outline. Two bugs fixed: (1) was feeding the COMPOSITED render (void baked in) as the red-box thumbnail so 50% zoom showed empty space inside the box - now feeds the RAW equirectangular day-map thumbnail (_ensure_raw_map_thumb, cached per map_set) which always fills the red box; (2) rewrote _paint into 3 layers: dimmed full map across widget, then monitors (screen-colour fill + full-bright map clipped to each monitor as a mask), then red outline of whole map on top. Pixel-verified: 100% map fills screen (corner=red outline), 50% map shrinks inside screen (corner=grey screen shows through, map fills red box), 200% map extends beyond screen (dimmed map + red outline visible outside monitor). Version 1.0.17.

## Phase 5 — Natural hazard overlays (2026-07-11)
- [x] **5.1 Earthquakes** — new hazards.py fetches USGS earthquake data
      (CDN summary feeds for standard buckets, FDSN query for arbitrary
      magnitude). GUI (Clouds & Weather tab): show-earthquakes checkbox,
      min-magnitude spinbox (0-9), time-window combo (hour/day/week/month).
      Drawn as magnitude-scaled circles, colour ramp yellow->orange->red->
      magenta. Cached 5min, last-known-good, backoff, never blocks render.
- [x] **5.2 Hurricanes** — hazards.py fetches NOAA NHC CurrentStorms.json;
      GUI checkbox. Drawn as category-coloured spiral glyph at storm centre
      + name/category label + forecast track polyline (antimeridian-split)
      where geometry available. Liberal field parsing (feed shape varies).
      Cached 30min, same robustness.
- [x] **5.3 Render integration** — _draw_hazards() layers above clouds,
      below city markers; threaded through all 3 render branches (mirror/
      span/independent). Worker fetches hazard data best-effort. Every
      hazard record wrapped so malformed data can't break a render.
- [x] **5.4 README + attribution** — documented both overlays; added
      hazards.py to module list; USGS + NOAA NHC public-domain attribution.
- Session 12 (2026-07-11): Phase 5 complete. Earthquake + hurricane overlays. Sources: USGS (earthquakes, free no-auth GeoJSON - summary feeds + FDSN query), NOAA NHC (hurricanes, CurrentStorms.json). New hazards.py mirrors clouds.py robustness (disk cache, last-known-good, backoff, atomic writes, never raises into renderer). Coords: USGS is [lon,lat,depth] epoch-ms; NHC coords parsed from both numeric and "23.4N"/"81.2W" string forms. Rendering: magnitude-scaled colour-ramped quake circles, category-coloured cyclone spirals with tracks. Layered above clouds/below cities. GUI controls on Clouds & Weather tab. Pixel-verified quake/hurricane colours; visual demo looked clean. Version 1.0.18. Network fetch fails gracefully (sandbox has no USGS/NOAA access; real machine will fetch live).
- Session 13 (2026-07-11): Hazard display customisation. render.DEFAULT_HAZARD_STYLE + settings['hazard_style'] dict threaded through whole render pipeline (all 3 branches). Earthquakes: marker shape (circle/ring/dot/cross), colour mode (magnitude ramp / custom hex), size mult, AND optional magnitude-number label with custom text colour + size (the headline request). Hurricanes: shape (spiral/ring/dot), colour (category/custom), size, toggle name label, toggle forecast track. New render helpers _draw_marker, _draw_hazard_label, _hex_rgba. GUI: full display-options rows on Clouds & Weather tab under each hazard, with QColorDialog swatches. _on_hazard_style_changed / _pick_hazard_color / _load_hazard_style_widgets / _refresh_hazard_swatches handlers. Pixel-verified magnitude number (green text detected), custom colours, all shapes; visual demo of magnitude labels looked clean. Version 1.0.19.
- Session 14 (2026-07-11): Two fixes. (1) Map position X/Y bug: same class as the old zoom bug - position reached the screen-area preview but not the desktop. Root cause: the render's span branch used monitor_layout.virtual_width/height for the compose canvas, but _render_layout() in mirror mode returned the DETECTED physical layout (e.g. 800x800) while the worker rendered at _current_resolution (e.g. 3840x2160) - mismatch meant wrong-sized output/void. Also position was absolute px, which doesn't scale between the small preview render and full render. FIX: (a) _render_layout() now synthesises a single-monitor layout at exactly the render resolution for mirror+placement; (b) render() now treats map_pos_x/y as FRACTIONS of virtual size (resolution-independent), worker converts stored px -> fraction via _pos_fraction(), screen-area preview converts px->fraction->preview-px consistently. Verified: 20% Y offset gives 100px bar at 1000x500 AND proportional 40px bar at 400x200 preview. (2) Hazard scan interval: hazards.py get_earthquakes/get_hurricanes now take ttl param; new "Check for new data every N min" spinbox (1-1440) on Clouds & Weather tab (setting hazard_scan_minutes, default 30). Between scans data served from disk cache - no re-download every wallpaper refresh. Version 1.0.20.
- Session 15 (2026-07-11): The REAL position bug fix. All previous changes made the RENDERED IMAGE correct (verified: 150px void bar at top when Y=150), but the desktop still didn't show it because wallpaper.py wasn't setting picture-options - so GNOME/etc used their default "zoom" mode which crops-to-fill, chopping off the void bar. Fixed in every DE branch: GNOME/Cinnamon/MATE now always set picture-options=stretched (or spanned in multi-monitor mode), KDE forces FillMode=0 (Stretch), XFCE sets image-style=3 (Stretched), swaybg uses `stretch` mode, feh uses --bg-scale instead of --bg-fill. All show the image at 1:1 pixel dimensions so any deliberate void bar from map_pos_x/y is preserved. Version 1.0.21.
- Session 16 (2026-07-11): THE actual root cause. The render was producing the void bar - but at the wrong resolution. When a layout was present, render() was using layout.virtual_width/height for the output size, IGNORING the caller's requested width/height. So preview renders (asked for 1280x720) were writing full-resolution 3840x2160 files. The bar WAS in there, at position 100/2160 = 4.6% of height, but when the preview label scaled that giant image down to fit, the bar became 1-2 pixels tall - invisible. Fix: render() now respects the caller's width/height as the compose canvas size; the layout only supplies the semantic (span vs independent). Position offsets are fractions of the target size. Also added _ScaledMonitor proxy for independent-mode preview scaling. Verified: preview at 1280x720 shows 33px red bar (100/2160 * 720 = 33.3); full render at 3840x2160 shows its own 100px bar; both consistent. Version 1.0.22.
- Session 17 (2026-07-11): Three fixes. (1) Custom (independent) mode X/Y not working: _render_monitor_view never read map_pos_x/y from the per-monitor config - only zoom. Added: pixel-to-fraction conversion using the underlying real monitor size (works with _ScaledMonitor proxy), then applied as an offset in all three zoom branches (>1 shifts crop, <1 shifts paste, ==1 pastes over void). (2) Mode dropdown stuck: removed the setMinimumContentsLength(38) + setMaxVisibleItems(10) that was a Windows-11-specific popup-height hack causing popup-not-showing on some Linux WMs. Replaced with the standard setSizeAdjustPolicy(AdjustToContents). Verified cycling mirror→span→independent→mirror→span→mirror all work. (3) Preview overlay for mismatched monitors: new preview_show_monitor_overlay render param, applied only for in-app preview (RenderWorker.is_preview=True). Dims off-monitor areas to 1/3 brightness and outlines each monitor rect in white on the composed preview, so users can see which parts of the map land on which physical screen and where gaps leave content invisible. Version 1.0.23.
- Session 18 (2026-07-11): Six related fixes/features. (1) Hot-plug monitor detection: new _install_hotplug_watchers hooks QGuiApplication.screenAdded/screenRemoved/geometryChanged signals AND runs a 3s poll timer as a fallback for tiling WMs that don't emit those. Layout re-detected + UI refreshed automatically. (2) X/Y position sliders: replaced the raw QSpinBoxes with LabeledSlider composites (slider + synced number box), range -3000..3000 px. (3) Mode-aware Screen Area preview: new _screen_area_layout filter - mirror shows only primary, custom shows only the monitor currently selected in "Editing monitor" combo, stretch shows all. Refreshed on mode change and active-monitor change. (4) Stretch mode aspect ratio: render() span branch now fits the map at natural 2:1 into the virtual desktop with void bars around it, rather than stretching to fill (which squashed the world on wide multi-monitor setups). (5) Softer preview overlay: off-monitor areas dimmed to 1/6 brightness instead of 1/3, so gaps read as clearly-inactive at a glance. (6) Update Now keeps overlay: _on_render_done now schedules a preview render after applying the full render, so the label re-acquires the monitor-overlay version instead of showing the raw wallpaper. Bonus: focal picker on Map & View tab now shows a caption ("Global centre" vs "Editing centre for monitor N") so users know what they're editing in custom mode. Version 1.0.24.
- Session 19 (2026-07-11): Screen-area edit widget now matches renderer in stretch mode. _update_screen_area_preview was still using the old vw*zoom / vh*zoom logic that stretched the map to fill the virtual desktop - so the red box showed a wide flat rectangle that didn't match what the renderer (which now fits map at 2:1 with void bars) actually drew. Fixed by branching on monitors_mode: in span mode compute fit_w=min(vw, vh*2) matching render.py, in mirror/custom keep vw*zoom for full-monitor fill. Also uses the widget's own filtered layout (screen_area_preview._layout) so it lines up with what's actually being displayed after the mode filter. Verified: 3200x1080 stretch at zoom 1.0 -> red box is (520, 0, 2160, 1080) exactly matching the renderer. Version 1.0.25.
- Session 20 (2026-07-11): Main preview now visually matches the edit widget's language. My analysis showed both were mathematically producing the same proportional layout (verified against 5 zoom/position combinations), but the user perceived a mismatch - likely because the two used different visual conventions (edit widget: red box on grey monitors; main preview: dimmed off-monitor + white outlines with no map outline). Added a red map-area outline to the main preview overlay via _apply_monitor_overlay now taking an optional map_rect param. render() span/independent branches record the placement rect as map_rect_for_overlay and pass it through. Now the main preview shows the same red box the edit widget does, on top of the actual rendered wallpaper - so any real misalignment (from math bug or user's monitor setup) is immediately visible as two boxes that don't line up, and confirmed-good renders show the two matching perfectly. Version 1.0.26.
- Session 21 (2026-07-12): Two enhancements to help pinpoint the reported "preview doesn't match monitors" discrepancy. (1) Preview overlay now draws numbered corner badges on each monitor (red badge with "1" for primary matching the edit widget's badge; grey badges for secondaries) plus primary-red / secondary-grey outline colours. That matches the Displays-tab edit widget's visual convention exactly, so any mismatch between the two views is immediately obvious by number ("monitor 2 sits here in the preview but there in the edit widget" - visible at a glance instead of comparing angles). (2) Added "Copy info" button next to "Refresh" on the Displays tab. It dumps the detected layout, current mode, resolutions, and monitor 0 config as plain text into the clipboard - so when a discrepancy IS real, the user can paste back exactly what the app thinks their layout is (vs what it should be), which pinpoints whether the bug is in detection, filtering, or rendering. Added __version__ to package so the diagnostic dump can include it. All analytical tests still show the preview and edit widget produce mathematically identical proportional layouts, so v1.0.27's tools should either (a) reveal the actual discrepancy to fix, or (b) show they now match and confirm nothing was broken. Version 1.0.27.
- Session 22 (2026-07-12): FOUND the real preview/screen-area mismatch, thanks to the user's diagnostic dump: Virtual desktop 5760x2160 with mismatched monitors (eDP-1 laptop 1920x1080 bottom-left + HDMI-1 4K 3840x2160 right). All along, the two views produced mathematically identical PROPORTIONAL layouts - but in ABSOLUTE widget-pixel positions they differed because (a) _fit_transform reserved a 24px margin around the layout (for zoom->100% overflow room), while the main preview overlay has NO margin, and (b) the widget's own aspect didn't match the virtual desktop's aspect, forcing centered horizontal/vertical padding. So a user overlaying the edit widget rectangles onto the preview saw monitor rects at different positions - the exact discrepancy reported. Fixed both: (1) removed the 24px margin - red map outline now clips to widget edges like it does in the preview, same visual convention; (2) added hasHeightForWidth+heightForWidth so the widget's height auto-matches its layout aspect (1280 wide with 5760x2160 layout -> widget takes 480 tall). Verified: user's exact setup now produces IDENTICAL monitor positions in both views: Monitor 1 at (0, 240, 427, 240) and Monitor 2 at (427, 0, 853, 480), pixel-perfect match. Version 1.0.28.
- Session 23 (2026-07-12): Five improvements. (1) Preview black border eliminated: preview_label had a dark background + 24px padding baked in as text-mode styling, which framed the pixmap when KeepAspectRatio left unused space in the label. Added _preview_text_style vs _preview_pixmap_style constants; swap to transparent-no-padding when a pixmap goes in, back to dark on spinner ticks. Also updated _update_preview_size to cap CONTAINER WIDTH when height gets capped by max_h, so aspect matches pixmap exactly (was letterboxing on very wide multi-monitor setups). (2) Removed redundant red "1" badge from the map area rectangle in the edit widget - the red outline suffices and the badge was easy to confuse with the primary-monitor "1" badge. (3) Low usage mode (setting: low_usage_mode): caps full-render at 1920x1080 (preview untouched), uses BILINEAR instead of LANCZOS, skips the unsharp-mask pass. Measured: 44% faster render at 1920x1080. Threaded via low_usage param through _render_map_image → _render_monitor_view → all three render() branches → worker → settings. (4) Pause on fullscreen (setting: pause_on_fullscreen): new fullscreen.py uses xprop to check _NET_ACTIVE_WINDOW → _NET_WM_STATE for _NET_WM_STATE_FULLSCREEN. Wired into trigger_update; skipped only on auto-tick paths (manual "Update Now" bypasses via _trigger_manual_update flag). Never raises - subprocess errors just fall through to normal render. (5) Export/Import settings: new settings.export_bundle / import_bundle helpers producing {"kind":"earthwall-settings-bundle","schema":1,"settings":{...},"cities":[...]}. Import confirms with QMessageBox and automatically writes settings-backup-<timestamp>.json before overwriting. Rejects non-EarthWall files with a friendly error. GUI: "Export…" and "Import…" buttons on a new "Settings backup" group on the General tab. Also fixed __init__ __version__ that got lost across recent versions. Version 1.0.29.
- Session 24 (2026-07-12): Longitude view + tab renames + focal picker removal. (1) New "Longitude view" LabeledSlider (-180 to +180°) on the Map View & Display tab, at the top of the map placement box. Scrolls which portion of the equirectangular map is centred - identical semantic to the removed focal dot's lon axis, but a clean single-axis slider. In mirror/stretch: sets global center_lon. In custom: sets the current monitor's per-monitor center_lon. Preset buttons (Americas / Atlantic / Asia / Pacific-Australia) moved to sit below the slider as quick-jumps. (2) Removed from Maps tab (formerly Map & View): the MapFocalPointPreview widget, the X/Y (lon/lat) spin inputs, the "Editing centre for..." label, and the presets that used to live here (moved to Displays). Also removed the four helper methods that supported them (_on_map_focal_changed, _on_focal_spin_changed, _refresh_focal_target_label, _jump_center_lon). Kept center_lat in settings (defaults to 0, no UI) so existing configs don't break. (3) Screen-area map thumbnail now rolls to match center_lon: new _roll_pixmap (uniform semantics with render._roll_longitude, pixel-verified consistent), cached by (map_id, center_lon) key with a separate base cache so longitude changes only re-roll (not reload). New _refresh_screen_area_thumb hooks into the longitude change so the edit widget's rolled map shows the same slice of the world the wallpaper does. (4) Tab renames: "Map & View" → "Maps", "Displays" → "Map View & Display". Verified with real map render at different center_lon values: pixel-identical results between renderer and widget roll semantics. Version 1.0.30.
- Session 25 (2026-07-12): Hot-plug freeze fixed. User reported: plugging a monitor into the running app freezes the display-mode dropdown until restart. Root cause was signal storm - screenAdded + geometryChanged + the 3s poll all firing within ~50ms of a hot-plug, each triggering a full _refresh_monitor_layout that clears/rebuilds the screen-area preview and monitor-editor combo. If the user was mid-click on the mode combo when the burst hit, the widget got rebuilt underneath the click and became stuck. Fixes: (1) new _layout_refresh_timer QTimer (singleShot, 300ms) that all four signal sources feed into via start() - burst of 10 signals coalesces into a single refresh 300ms after the last one. Verified: 10 rapid _on_screens_changed calls => timer stays active, fires once ~310ms later. (2) _refresh_monitor_layout now checks whether monitors_mode_combo's popup is visible and defers itself via QTimer.singleShot(200) if so - never rebuilds under an open dropdown. (3) _rebuild_monitor_editor_combo does the same for its own popup, and preserves the previous selection across rebuilds so users don't get bumped back to monitor #1 by an unrelated hot-plug. (4) _active_monitor_index is now bounds-safe: after a hot-unplug that made the combo point at a monitor that no longer exists, falls back to the primary (else first) monitor instead of returning a dangling index that would spawn stale configs on subsequent reads. Real xcb test with 10-signal burst passes cleanly - mode combo enabled, cycles mirror→span→mirror, no crashes. Version 1.0.31.
- Session 26 (2026-07-12): Profile support. New earthwall/profiles.py: thin wrapper over settings.export_bundle/import_bundle. Each profile = one JSON file under ~/.config/earthwall/profiles/<name>.json in the same schema as export bundles, so profiles/backups/imports are all interchangeable. Public API: is_valid_name, list_profiles, load_profile, save_profile, delete_profile, rename_profile. Name validation regex `^[\w\-. ]{1,64}$` rejects path traversal and reserves reasonable filesystem chars. New `active_profile` setting (empty = unsaved working config). General tab now has a "Profile" group at the top with a QComboBox listing all profiles + a synthetic "(unsaved working config)" entry, plus Save / New… / Rename… / Delete buttons. Handlers: _refresh_profile_combo, _on_profile_selected (with confirmation dialog and auto-backup), _save_active_profile (overwrites current profile with in-memory state), _create_new_profile (QInputDialog for name, validation, overwrite confirmation), _rename_active_profile, _delete_active_profile. Rename/Delete/Save buttons disable when on the (unsaved) entry. _load_settings_into_ui calls _refresh_profile_combo so the dropdown syncs on init and on Import. Verified: round-trip save/load/rename/delete via API, GUI shows active profile correctly, button enable/disable follows selection state. Version 1.0.32.
- Session 27 (2026-07-13): Tile map for ultrawides. New per-monitor setting tile_map (in monitor_configs, following the same pattern as zoom / map_pos / void_fill_color). When on, the 2:1 equirectangular map is repeated horizontally to fill leftover space instead of showing void bars - the seam is invisible because the right edge of the map is the same longitude as the left edge. Three render code paths updated: (1) _compose_multi_monitor gained a tile_map param that pastes additional map copies left/right of the placement until virtual_w is covered; skips the "fully covers" fast path when tiling is requested (needs the void canvas as the target for the tile pastes). (2) _render_monitor_view got a tile_map branch that renders the map at NATURAL 2:1 aspect fit to monitor height (instead of stretched to monitor size) and paste-tiles it horizontally over the monitor's void fill. Focal lat and position offsets still apply. (3) render() signature adds tile_map bool; the span branch passes it through to _compose_multi_monitor; the classic mirror-without-layout branch is untouched (rare, and mirror-with-layout already routes through span/compose). Worker reads tile_map from monitor_configs["0"] just like the other per-monitor settings. GUI: new QCheckBox "Tile map horizontally..." below the zoom slider on the Map View & Display tab, persisted via _on_settings_changed like zoom/pos, loaded on active-monitor-change and full settings-load. Screen-area edit widget: added _tile_map field + set_tile_map() + _draw_map_at() helper that centralizes the "paint map thumbnail, optionally tiled" logic and is used in both Layer 1 (dim map outside monitors) and Layer 2 (bright map inside monitors) - so tiling shows up in both the "behind" and "on" the monitors, matching what the wallpaper will look like. Main window pushes the current monitor's tile_map into the widget in _update_screen_area_preview. Pixel-verified: seam between tiles is diff=0 (antimeridian wraps cleanly); ultrawide render at 32:9 shows map filling everywhere vs void bars when off. Version 1.0.33.
- Session 28 (2026-07-13): Center the main preview. When the preview container's max-width gets capped (very wide multi-monitor setups where preserving aspect requires shrinking width), it was hugging the left edge because QVBoxLayout defaults to left-alignment. One-line fix: pass alignment=Qt.AlignHCenter when adding the container. Verified with a simulated 5120x1440 ultrawide layout: container centered at x=690 in a 1382px group box (both math and actual pixel position match). Version 1.0.34.
- Session 29 (2026-07-13): Two follow-ups from the last change. (1) Preview sliver fix: the v1.0.34 centering fix only set setMaximumWidth on the container. With alignment=Qt.AlignHCenter in place, Qt stops stretching the container to fill and instead uses its size hint - which for a maxWidth-only-set widget is 1px - collapsing the preview to a vertical sliver. Rewrote _update_preview_size to compute the exact target_w and target_h from the same aspect calc and use setFixedSize(target_w, target_h). Now the container has a real size in both the natural-height and height-capped cases, centering works, and aspect always matches the wallpaper. Verified 16:9 (533x300) and 32:9 ultrawide (1066x300), both centered, both correct aspect. (2) Removed the red map-area outline in both the main preview overlay (render._apply_monitor_overlay layer 4) and the Map View & Display screen-area edit widget (layer 3) - the map's extent is already obvious from where the map thumbnail vs the monitor-grey backgrounds show through, and the outline was creating a distracting red rectangle across otherwise-clean map content. Version 1.0.35.
- Session 30 (2026-07-13): Startup-in-tray ignoring Map View & Display settings. Root cause: monitor detection was gated behind showEvent (comment claimed "QApplication.screens() only returns useful geometry once the app is actually on screen"). When the user has Start-in-tray on, showEvent never fires before the initial trigger_update at 500ms, so _current_layout stays None; render() then falls through to the classic single-image mirror path that ignores every per-monitor setting (zoom, position, tile_map, void_fill). Only after the user surfaced the window from the tray did showEvent fire, populate the layout, and Update Now finally use the real settings. Two fixes: (1) Moved _refresh_monitor_layout() + _install_hotplug_watchers() from showEvent to __init__, right after _load_settings_into_ui. In practice QScreen geometry is available immediately after QApplication is constructed; the "wait for showEvent" idiom was overly cautious. showEvent still guards behind _monitors_detected_once so it doesn't re-run. (2) _render_layout was only forcing the synthetic-layout path for non-default zoom / map_pos - missed tile_map, void_fill_color, and void_fill_image (added later). Extended the check to include all per-monitor settings that CAN'T be expressed by the plain mirror render, so any of them trigger the layout-aware render path. Verified: tray-startup with tile_map=True + zoom=1.34 + void_fill_color=#FF0000 now produces the correct render immediately, no need to hit Update Now. Version 1.0.36.
- Session 31 (2026-07-18): Three requested improvements. (1) Expanded city database from 118 to 495 cities across all continents (major world cities, capitals, and many mid-size cities people have personal ties to). Rewrote city_database.py: added _fold() for accent-insensitive matching (NFKD decomposition + strip combining marks, so "sao"->São, "zurich"->Zürich, "cordoba"->Córdoba), city_label() returning "City, Country", 3-tier ranked search_cities() (name-startswith > name-contains > country-contains, preserving importance order within tiers), and find_city() to resolve a label or bare name back to a row. (2) City search now shows "City, Country" so identically-named places (several Victorias, San Joses, Cordobas) are distinguishable. gui_city_dialog.py: completer model is now the full "City, Country" label list with MatchContains filtering; returnPressed resolves the top hit for keyboard users; _on_pick_from_search + new _on_search_enter use find_city/search_cities. (3) On-demand NASA map downloads to keep the tool small. New map_downloads.py: catalog of 13 downloadable maps (12 Blue Marble Next Generation monthly composites + Black Marble 2012 night lights). Monthly composites use NASA SVS's verified single-directory source (svs.gsfc.nasa.gov/.../a012564/frames/5400x2700_2x1_60p/world.topo.2004-MM.png) rather than guessed per-month Earth Observatory record IDs - one clean directory, sequential filenames, all 12 confirmed present via web fetch. Downloads stream to a temp dir, validate+convert PNG->JPEG q92 (8-9MB PNG -> ~2-3MB JPEG, ~4x disk saving), and move into place atomically so a failed/cancelled download never leaves a broken map. New gui_map_download_dialog.py: MapDownloadDialog grouped by category with per-map checkboxes, select-all/none, progress bar, and a background _DownloadWorker QThread (cancellable on close). Already-installed maps shown greyed with "installed". "Download NASA Maps…" button added to the Maps tab; _on_download_maps refreshes the map list if anything landed. Full pipeline verified with mocked fetcher: download->progress->verify->convert->install->appears-in-list->re-download-blocked->corrupt-rejected->no-partial-left. NASA blocked in sandbox so live download untested, but all URLs verified against NASA's actual directory listings. Version 1.0.37.
- Session 32 (2026-07-18): Five requested improvements. (1) MASSIVE CITY EXPANSION: 118 -> 32,444 cities via geonamescache. Includes Lake Jackson, Texas (pop 27,533) and everything else down to ~15k pop. Ships as gzipped-JSON data file (`earthwall/data/cities.json.gz`, ~578 KB) generated by scripts/gen_cities_data.py. city_database.py rewritten: lazy-loads on first search, LazyCityList proxy for old CITY_DATABASE iterators, precomputed folded-name cache for fast repeat searches. New row shape (name, country, admin, lat, lon, tz) with admin1 for US/CA/AU/BR/MX/IN/DE/GB/CN. US state code->name mapping so "Lake Jackson, TX" displays as "Lake Jackson, Texas, United States"; CA provinces and AU states get their full names too; other countries show just the code. Same-named cities now distinguishable ("Springfield, Illinois" vs "Springfield, Missouri" etc). Cold cache load ~124ms one-time, subsequent searches ~10-12ms. Dialog pre-warms via QTimer.singleShot(0) so first keystroke doesn't hit the cold cost. gui_city_dialog completer switched from static 32k-label model to dynamic model driven by textEdited -> our search_cities() -> top 40 hits, keeping popup snappy. (2) WEATHER GLYPHS FIXED: all U+1F3xx weather emojis (Mostly clear = 🌤 rendering as a square, etc) replaced with BMP-only Miscellaneous Symbols block glyphs that DejaVu Sans / Noto / Liberation reliably include: ☀ ⛅ ☁ ☂ ☔ ❄ ⚡. Verified each of the 28 WMO glyphs renders as real ink pixels in DejaVu Sans. (3) EARTHQUAKE LABELS + CLUSTERING: "Show magnitude number" checkbox replaced with a "Text next to marker" combo offering None / Magnitude / Place / Time (UTC) / Magnitude+place / Magnitude+time. USGS "12 km NE of Anza, CA" place strings get the leading distance stripped so the label reads as a place name. Time renders as "DD Mon HH:MM UTC". New "Group overlapping quakes" toggle with cell-size spinbox (0.5°-20°, default 4°): buckets quakes into a lat/lon grid and keeps only the largest magnitude quake per cell. Aftershock sequences and Ring-of-Fire clusters no longer make the map into a red blob. Legacy eq_show_magnitude bool still honoured for backward-compat. (4) PROFILE FILE IMPORT/EXPORT: "Import from file…" and "Export to file…" buttons added below the profile dropdown on the General tab. Export reads the profile from disk (not in-memory state, so unsaved edits aren't exported) and writes it as a standard bundle JSON. Import shows a QInputDialog to name the resulting profile (defaulting to the file's stem, minus "earthwall-profile-" prefix), validates the name, and offers to overwrite if the name is taken. Uses the same bundle format as the whole-settings backup so files are interchangeable. Export button disables when no real profile is selected. (5) SEASONAL AUTO MAP MODE: new special map_id "seasonal_auto" that resolves to bmng_MM matching the current calendar month at each render call. Added as the top entry in the Maps tab list, with a label showing "X/12 installed" so users know they still need to download the rest. Falls back through the nearest installed BMNG month if the exact current month isn't available yet, so partial installs still work. New "Select all 12 Blue Marble months (for Seasonal mode)" quick-action button on the download dialog. Bug found + fixed: `import earthwall.render as X` gives the render FUNCTION because __init__.py re-exports it; used sys.modules['earthwall.render'] to get the submodule with SEASONAL_AUTO_ID. All 5 items verified with syntax/lint/xcb pass. Version 1.0.38.
